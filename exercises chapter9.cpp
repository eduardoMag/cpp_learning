#include <iostream>
int main(){
    //standard input
    std::cout << "PLease enter a number";
    int x;
    std::cin >> x;
    std::cout << "You entered: " << x;
    
    //two inputs
    std::cout << "Please enter 2 interger numbers: ";
    int x;
    int y;
    std::cin >> x >> y;
    std::cout << "you entered: " << x << ", and " << y;
    
    //multiple inputs
    std::out "please enter a char, int and a double: ";
    char c;
    int x;
    double d;
    std::cin >> c >> x >> d;
    std::cout <<"you entered: " << c << ", " << x << ", and " << d;
    
    //Write a program that accepts two int numbers, sums them up, and
    //assigns a result to a third integer. Print out the result afterward.
    std::cout << "please enter dos integers: ";
    int x;
    int y;
    std::cin >> x >> y;
    int z = x+ y;
    std::cout << "the sum is: " << z;
    

//Write a program that defines an int variable called x with a value of 123, post- 
//increments that value in the next statement, and adds the value of 20 in the following 
//statement using the compound assignment operator. Print out the value afterward.
            std::cout << "

//Write a program that divides numbers 9 and 2 and assigns a result to an int and a 
//double variable. Then modify one of the operands, so that is of type double and observe 
//the different outcomes of a floating-point division where at least one of the operands is 
//of type double. Print out the values afterward.

    }
